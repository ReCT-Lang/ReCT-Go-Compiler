var byte coolByte;
var int  coolInt;

coolByte <- 20;
coolInt  <- 10;

var varSelect <- false;

Print(string(varSelect ? coolByte : byte(coolInt)));

var str0 <- "yes";
var str1 <- "nah";

while(true)
{
	Print(varSelect ? "yes" : "nah");
	Sleep(1);
}